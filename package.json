{
  "name": "dealpilot",
  "private": true,
  "version": "0.0.0",
  "description": "DealPilot – real-estate deal analysis (monorepo root)",
  "packageManager": "pnpm@11.3.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
